import React from 'react';

const Follower = props => {
    return (
        <div>
        {/* {props.login.map(logCard => (<p>{logCard}</p> ))} */}
        HELLO
        </div>
      );
};

export default Follower;